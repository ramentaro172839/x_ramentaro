# CLAUDE.md

このプロジェクトで作業する Claude (AI) 向けの指示書です。

## プロジェクトの目的

<!-- TODO: プロジェクトの目的を具体的に記入してください -->
- x_ramentaro プロジェクト
- 依存パッケージ: `notebooklm-py`（詳細は `requirements.txt` 参照）

## フォルダ構成

- `src/` : 作業ファイル（実際に手を加えるコード・スクリプト）
- `docs/` : 参考資料（仕様書、メモなど。基本的に読み取り専用）
- `output/` : AIの出力先（生成物はここに書き出す）
- `.claude/commands/` : よく使う指示のテンプレート（`/コマンド名` で呼び出し）

## 守ってほしいルール

- 変更は基本的に `src/` 配下に対して行う
- 参考資料は `docs/` に置き、AIの出力・生成物は `output/` に保存する
- 依存パッケージ（`requirements.txt`）のバージョンを無断で変更しない
- 大きな変更・破壊的な操作（force push, reset --hard など）の前には必ず確認を取る

## やっちゃいけないこと

- `docs/` の参考資料を勝手に書き換えない
- 明示的な指示なしに `output/` 以外の場所へ最終成果物を書き出さない
- 無断でファイルを削除しない

## 出力ルール

- 基本的に日本語で回答する
- カジュアルなトーンで
- コードには必要に応じてコメントを入れる
